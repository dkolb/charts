apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "crafty-control-mc.fullname" . }}-test-connection"
  labels:
    {{- include "crafty-control-mc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "crafty-control-mc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
